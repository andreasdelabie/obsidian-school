filters:
  and:
    - file.name == file.folder
    - file.name != "_Class"
views:
  - type: cards
    name: Classes
    order:
      - file.name
      - teacher
    sort:
      - property: file.name
        direction: ASC
    imageAspectRatio: 1
    cardSize: 250
